# The deterministic UGSci core has no mandatory third-party dependency.
# Optional Providers are listed in requirements-optional.txt and are probed
# at runtime.  Keeping them out of this file prevents plugin startup from
# downloading large scientific stacks or failing when the network is offline.
