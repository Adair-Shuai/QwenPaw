name: Pre-commit Checks

on: [push, pull_request]

jobs:
  spam-gate:
    name: PR Spam Gate
    if: github.event_name == 'pull_request'
    uses: ./.github/workflows/pr-spam-gate.yml
    with:
      author: ${{ github.event.pull_request.user.login }}

  run:
    needs: [spam-gate]
    if: |
      always() &&
      (needs.spam-gate.result == 'skipped' || needs.spam-gate.outputs.blocked != 'true')
    runs-on: ubuntu-latest
    strategy:
      fail-fast: true
    env:
      OS: ubuntu-latest
      PYTHON: "3.11"
    steps:
      - uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Update setuptools and wheel
        run: |
          pip install setuptools==68.2.2 wheel==0.41.2

      - name: Install QwenPaw with dev dependencies
        run: |
          pip install -q -e ".[dev]"

      - name: Install pre-commit hooks
        run: |
          pre-commit install

      - name: Run pre-commit
        run: |
          pre-commit run --all-files 2>&1 | tee pre-commit.log || true
          echo "=== HOOK RESULTS ==="
          grep -E "(Passed|Failed|Skipped)$" pre-commit.log || true
          echo "=== FAILURE DETAILS ==="
          grep -A5 "Failed" pre-commit.log || true
          if grep -q Failed pre-commit.log; then
            exit 1
          fi
